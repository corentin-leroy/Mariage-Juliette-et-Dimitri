// Site mariage Juliette & Dimitri — JS vanilla
// Briques à implémenter (voir prompt Claude Code) :
//
// 1. Compte à rebours J-XXX jusqu'au 14 juillet 2027 (hero).
// 2. Nav : lien actif via IntersectionObserver + menu burger < 640px.
// 3. Formulaire RSVP : logique conditionnelle (présence Oui/Non, bébés → lits parapluie),
//    honeypot, validation, envoi fetch vers Sheet Monkey, écran de confirmation, gestion d'erreur réseau.
// 4. Scroll doux vers #rsvp depuis les CTA.

document.addEventListener('DOMContentLoaded', () => {
  // TODO
});
